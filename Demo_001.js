// Variablen und Konstanten

// Im Folgenden deklarierte und initialisiere ich Zahlenvariablen.
// In JavaScript gibt es für Zahlen den Datentyp Number.

let a = 200             // Veränderbar
console.log(a)          // 200

a = 100                 // Neuzuweisung
console.log(a)          // 100

const b = 200           // Nicht veränderbar
console.log(b)          // 200

b = 100                 // Nicht erlaubt => Error
console.log(b)          // Wird nicht mehr ausgeführt
